# June 2026 Target

Status: canonical target, not a current-state claim. The production matrix tracks the unproven parts until browser, backend, eval, and media gates pass.

## Thesis

NodeRoom is a finance-grade collaborative workroom where humans and agents safely produce auditable Excel-like work products: models, proofs, memos, charts, citations, traces, and exports.

The durable product mental model:

> Binder tells you what exists. Stage shows the work. Copilot drives the work. Status proves what just happened.

The durable agent mental model:

> LLM routes intent. Harness schedules work. Ledger commits truth.

## Desktop Shell Contract

The target desktop shell is four roles, not four equal cockpits:

| Surface | Owns | Must not own |
|---|---|---|
| Room Binder / Deal Binder | Source files, workbooks, work products, people, agents, review/proof, permissions | Full agent streams, queues, logs, protocol labels, or low-level tool controls |
| Work Surface | Spreadsheet, proof, source document, memo, chart, trace artifact, gold comparison | Permanent chat dominance or unrelated room navigation |
| Copilot | Chat, work queue, agent steering, active agent streams, artifact cards, task controls | Canonical spreadsheet state or hidden durable writes |
| Bottom Signal Tape + Status Strip | Ambient events plus short authoritative truth for commits, conflicts, evals, sync, cost, and runtime | Large proof/source drawers or permanent competing work panes |

The target can use three columns on desktop only when the left column is a stable binder, the center is the primary work surface, and the right side is Copilot/orchestration.

## Binder Contract

The binder is navigational. It answers "what exists in this room?"

- Source Files
- Workbooks
- Work Products
- People
- Agents
- Review & Proof
- Permissions

Click behavior:

- Clicking an artifact opens it on the center stage.
- Clicking a person jumps to their current location or visible work context.
- Clicking an agent highlights the claimed range and opens Copilot detail.
- Clicking a privacy or policy item opens the proof/policy artifact.

Anti-patterns:

- Do not turn the binder into a second agent cockpit.
- Do not put full streams, logs, queues, or protocol implementation labels in the binder.
- Do not make the binder compete with the work product for attention.

## Work Surface Contract

The center is where professional work is inspected and edited. Sources, proofs, traces, and gold comparisons open on the stage or as a center split within the stage, not as a new global panel.

For finance and GTM workflows, the stage must preserve spreadsheet muscle memory:

- keyboard-first navigation and edit modes,
- formula/value distinction,
- evidence overlays that do not break the grid,
- source/proof panes that can be opened just-in-time,
- no-clobber conflict feedback next to the affected work.

## Copilot Contract

Copilot owns conversation and operation steering:

- public and private chat modes,
- work queue,
- active agent streams,
- agent cards,
- task controls,
- plan previews,
- cancellations, retries, priorities, and clarification.

The agent may recommend command-like intent. The harness validates, schedules, and commits safe mutation-like writes. Selection, viewport, cursor, and presence remain ephemeral operation-like state.

## Bottom Contract

The bottom is not a drawer by default.

- Signal Tape is ambient and configurable. It can show source updates, agent events, risk, eval, cost, or collaboration activity.
- Status Strip is authoritative and short. It tells what just happened: committed, conflict, draft, synced, failed, retried, budgeted, or paused.
- Detailed proof/source/gold/trace views open on the center stage.

## Responsive Contract

| Width | Target behavior |
|---|---|
| `>=1440px` | Full binder, center work surface, right Copilot, bottom status |
| `1200-1439px` | Narrow binder or icon rail with peek behavior |
| `900-1199px` | Binder behind a Room button; center and Copilot remain usable |
| `<900px` | Mobile sheet/tabs pattern with one primary surface at a time |

## Agent And Scheduler Invariants

- Every user message becomes an intake event: new command, steering patch, parallel subagent, wait, clarification, note, cancel, or priority change.
- Expensive work gets a preflight PlanPreview: target artifacts, read/write/affected sets, dependency expansion, conflicts, scheduling recommendation, cost, and runtime.
- Affected sets include formulas, charts, memos/wiki refs, source refs, active human edits, proposals/drafts, agent claims, and privacy boundaries.
- Agents propose commands. The harness turns safe parts into mutations. Ephemeral collaboration stays as operations.
- Soft intent claims and short commit leases protect collaboration without blocking human typing.
- The LLM must not directly schedule, lock, interrupt, or write durable cells.
- Managed writes own lock, CAS, proposal, review, and trace behavior.
- Chat claims are not evidence. Durable evidence must point to source artifacts, cells, pages, rows, boxes, traces, or validator output.
- Formula cells must not be overwritten with scalar text unless the operation is explicitly a formula replacement.

## Workbook Runtime Target

Long term, the spreadsheet surface needs a real workbook runtime rather than a hand-built table:

- workbook runtime for local edit semantics, formulas, selection, fill, and overlays,
- Convex ledger for canonical room truth, CAS, locks, proposals, traces, permissions, and sync,
- NodeAgent harness for context, scheduling, provider calls, evidence, and commits,
- headless/worker validation for formulas and export/reopen checks.

This is an adapter target, not a commitment to one runtime until the POC passes.

## Evaluation Target

NodeRoomBench should score the product as a harnessed collaborative work system, not as a raw model.

Evaluate:

- final workbook correctness,
- formula correctness,
- formatting and usability,
- source citations and evidence,
- no-clobber behavior,
- multi-agent scheduling and affected-set planning,
- privacy boundaries,
- cost and runtime,
- trace quality,
- final artifact usefulness to a finance/GTM professional outside NodeRoom.

Official benchmark mode is stricter than NodeRoomBench. BankerToolBench and
SpreadsheetBench readiness must be tracked by the official benchmark gate, not
inferred from internal professional workflow evals. That gate has to prove
official task ingest, hidden-gold isolation, runner adapters, trajectory/cost
capture, workbook export/reopen fidelity, and benchmark-native scoring before
the README can claim benchmark readiness.

Allowed and required reporting:

- model,
- harness,
- tool policy,
- budget,
- evaluator,
- retries/failures,
- route,
- final artifact,
- trajectory.

Disallowed:

- hidden gold access,
- benchmark-specific hardcoding,
- evaluator or test mutation,
- public answer lookup,
- tuning on held-out cases,
- hiding retries or failures,
- reporting raw model capability when the harness did the reliability work.

## Current Repo Fit

Built or close:

- left rail already carries files, upload, people, and public agent presence;
- the shell now uses the target surface roles: Room Binder, center Work Surface, right Copilot, and shell-level Signal Tape/Status Strip;
- center-stage split mode exists in memory mode so a secondary proof/source/wiki surface can sit beside the primary work surface without moving into Copilot or the bottom strip;
- public/private chat lanes now live in Copilot tabs;
- resizable panels exist;
- public/private chat and artifact surfaces exist;
- deterministic `IntakeDecision` and `PlanPreview` contracts classify messages, expand affected sets, and block privacy/formula/budget unsafe plans before provider spend;
- trace/proposals/auto-accept exist;
- managed lock/CAS/draft behavior and model ladder evals exist;
- public-gold, finance, professional workflow, and QA matrix infrastructure exist.
- official benchmark readiness reporting now exists and is wired into the agent
  improvement loop / HALO deterministic lane.
- BankerToolBench official local-bundle ingest exists for already-downloaded
  `tasks.jsonl` / `task-data` / `golden-outputs` bundles. It parses input files,
  workflow taxonomy, and weighted rubric metadata while keeping rubric, canary,
  and golden-output paths out of the agent-facing task payload.
- BankerToolBench sandbox staging exists: each task gets an `agent/` manifest
  with only the official default `final_prompt` and input files, plus a separate
  `evaluator/` manifest with prompt context, formatting context, canary,
  weighted rubric, golden outputs, and expected deliverable package metadata. A
  local BTB-shaped smoke fixture verifies zero agent-side golden/rubric/canary
  leaks.
- BankerToolBench local runner plumbing exists: `npm run
  benchmark:bankertoolbench:run` emits candidate deliverables from an
  agent-only workspace before opening evaluator-only weighted rubric and golden
  files, validates exact expected package shape for supported spreadsheet, deck,
  document, PDF, CSV, and image deliverables, and records missing/extra/duplicate
  or unsupported files. Excel deliverables are reopened and compared with the
  workbook scorer, so semantically matching `.xlsx/.xlsm` files can pass even
  when package metadata changes the hash. The local exact-package /
  exact-or-workbook-semantic verifier smoke proves the runner/scorer boundary
  and trace shape, not official Harbor/Gandalf scoring. `npm run
  benchmark:bankertoolbench:proof` now enforces staged isolation,
  candidate-before-evaluator trajectory, weighted-rubric/package accounting,
  supported deliverable policy, and 0-leak artifacts in HALO.
- SpreadsheetBench V1/V2 official local-bundle ingest exists, with agent-facing
  inputs/prompts separated from evaluator-only golden workbooks and scorer
  metadata. Smoke artifacts cover the V1 verified-400 bundle and V2 public
  example bundle.
- SpreadsheetBench sandbox staging exists: each task gets an `agent/` manifest
  with only instructions, prompts, and input workbooks, plus a separate
  `evaluator/` manifest with golden files and scorer metadata. Smoke artifacts
  verify zero agent-side gold/scorer leaks for V1/V2 samples.
- SpreadsheetBench workbook scoring exists for local candidate-vs-gold baselines:
  values, formulas, optional stable style fingerprints, answer-range column/row
  layout, and merge ranges can be compared after reopening `.xlsx` files. Smoke
  artifacts compare official input workbooks against evaluator-only gold to prove
  the diff path; they are not model scores.
- SpreadsheetBench staged runner plumbing exists: a copy-input baseline reads
  only the staged `agent/` manifest, emits a candidate workbook, then opens the
  `evaluator/` manifest for scoring. Smoke artifacts record trajectory, timing,
  model-call count, provider cost, and failed baseline scores. This is runner
  plumbing, not a model/export worker.
- SpreadsheetBench edit/export plumbing exists: `--mode apply-agent-patch` reads
  `agent/edit-plan.json`, applies cell-level value/formula/style operations,
  writes a candidate workbook, then opens the evaluator manifest for scoring.
  A smoke artifact records a passing edited workbook with zero mismatches. This
  proves workbook edit/export/reopen packaging; it is still not a model score.
- SpreadsheetBench model-edit plumbing exists: `--mode model-edit-plan --model
  <route>` snapshots only the staged `agent/` workbook and prompts, asks the
  configured model for a JSON edit plan, applies the plan, records token/cost
  usage, emits a candidate workbook, then opens the evaluator manifest for
  scoring. A one-task live smoke with `gpt-5.4-nano` passed and recorded model
  call, trajectory, timing, and cost. This is still not an official benchmark
  score until it runs against official bundles under the documented policy.
- SpreadsheetBench model-edit prompts now include agent-visible table block
  summaries: contiguous section ranges, detected titles, header rows, headers,
  and data-row counts. This is derived only from the input workbook, not gold or
  evaluator answer metadata.
- SpreadsheetBench model-edit plans now support a bounded `aggregate_section`
  operation for visible table tasks: group source-section rows by named headers,
  sum a value column, sort by keys, rewrite the matching target section, and
  cache the total result. The prompt exposes candidate section operations only
  from agent-visible workbook structure; unsupported invented operations such
  as `clear_section` are dropped; section rewrites apply after scalar cell edits
  so a later bad scalar edit cannot clobber the deterministic table rewrite.
- SpreadsheetBench model-edit plans now also support bounded structural
  operators for two additional official V1 patterns: `filter_rows` copies
  concrete rows from an agent-visible source range when a visible date column is
  between visible start/end criteria cells, and `sort_unique_rows` skips
  blanks/headers, removes duplicate key rows, sorts by a visible key column, and
  writes the full target table. These operators are exposed as
  agent-visible candidates and are re-inferred during normalization, so the
  harness can repair fragile dynamic-array formulas or short prefix writes
  without reading evaluator gold.
- SpreadsheetBench runner attempts now create an explicit `agent-workspace/`
  before candidate generation. Candidate generation reads copied agent-visible
  manifests, workbooks, prompts, and edit plans from that workspace; each retry
  records a workspace manifest in the trajectory, and contamination scans cover
  those manifests. The full verified-400 V1 stage now records `400/400` tasks
  staged, 800 agent-facing files, 400 evaluator gold files, clean isolation
  counters, and 0 leaks across 800 checked files. The V2 public example stage
  proof records 3 paired input/gold tasks staged from 26 example tasks, clean
  isolation counters, and a contamination pass. A local Node permission subprocess smoke also proves an
  agent process can read its copied workspace file and is denied evaluator-only
  gold outside that root. This proves the runner's local file-view boundary; it
  is not yet Docker/Harbor process isolation, network isolation, or a resource
  sandbox. `npm run benchmark:docker-sandbox:probe` now records the stronger
  boundary separately in `docs/eval/docker-sandbox-probe.json`; the current
  artifact reports Docker CLI present but daemon unavailable, so this remains
  blocker evidence rather than readiness proof.
- SpreadsheetBench model-edit snapshots now use a larger bounded workbook
  context so medium official workbooks are not silently truncated, and the
  runner caches deterministic local formula results during export/reopen for
  arithmetic, same-sheet refs/ranges, `SUM`/`AVERAGE`/`MIN`/`MAX`/`COUNT`/`COUNTA`,
  `ABS`, `ROUND`/`ROUNDUP`/`ROUNDDOWN`, `IF`/`IFERROR`, single-criteria
  `SUMIF`/`COUNTIF`/`AVERAGEIF`, and multi-criteria
  `SUMIFS`/`COUNTIFS`/`AVERAGEIFS`, plus exact
  `MATCH`/`INDEX`/`VLOOKUP`/`XLOOKUP`, `SUMPRODUCT`, text extraction/search
  (`LEFT`/`RIGHT`/`MID`/`LEN`/`FIND`/`SEARCH`/`REPLACE`), `TEXT`/`DATE`,
  `VALUE`, `CONCATENATE`, and `TRIM`, including basic wildcard criteria.
  The scorer also treats candidate formulas as value-equivalent when evaluator
  gold stores only a scalar result, while still enforcing formula equality when
  the gold cell contains a formula. This improves harness fidelity without
  adding answer lookup, but it is not a complete Excel calculation engine:
  approximate lookup, array formulas, volatile functions, external refs, and
  dynamic Excel functions remain outside the local deterministic subset.
- SpreadsheetBench format scoring now checks stable cell style fingerprints
  plus answer-range column widths/hidden state, row heights/hidden state, and
  intersecting merge ranges when `--compare-styles` is enabled. This makes
  layout drift visible, but it is not a complete official formatting or rendered
  visual policy.
- SpreadsheetBench repeated-run accounting now exists: `--repeats 5` records
  case count, attempt count, pass rate, p50/p95/max latency, failure taxonomy,
  model calls, tokens, and provider cost. The checked-in official V1 N=5 smoke
  now records 5 attempts on one task, 5/5 pass, p95 4.593s, $0.01059125 spend,
  zero failure counts, average overall 1.0, and 0 candidate-output leaks after
  sheet-alias, JSON-drift, table-block prompt, `aggregate_section`, unsupported
  op repair, operation-ordering, and formula/scalar scoring repair. This proves
  one official-task smoke path, not full benchmark readiness.
- SpreadsheetBench V1 now has a broader three-task repeated live smoke on the
  locally staged official tasks. `docs/eval/spreadsheetbench-v1-model-edit-plan-3task-n5-live-smoke.json`
  records 15 attempts across 3 cases and 5 repeats, 15/15 pass, average overall
  1.0, p95 5.080s, $0.0462905 spend, zero failure counts, zero retry attempts,
  0 candidate-output leaks across 75 checked files, and result-level SHA-256
  sidecar evidence for candidate manifests, agent-workspace manifests,
  generated edit plans, and raw model outputs after adding `filter_rows` and
  `sort_unique_rows`. `npm run benchmark:spreadsheetbench:proof` now enforces
  these artifact claims plus candidate-before-evaluator trajectory order and is
  wired into HALO. This proves the harness can recover three distinct official
  V1 operation shapes repeatedly under live model variance in the current
  staged corpus with auditable sidecars; it is still not a full held-out bundle
  run.
- SpreadsheetBench retry-policy accounting now exists: `--retry-failed N`
  retries candidate-generation and scoring errors, `--retry-score-failures`
  explicitly opts into retrying scored-but-wrong candidates, and the report
  records case-level attempts, retry exhaustion, pass-after-retry counts,
  latency, token use, and provider cost. The checked-in official V1 retry smoke
  ran one task with `gpt-5.4-nano`, `--retry-failed 2`, and
  `--retry-score-failures`: all 3 attempts reached scoring, best overall was
  0.616667, p95 latency was 11.033s, spend was $0.0095201, and pass remained
  0/3.
  This proves auditable retry behavior and the current planner gap at the same
  time.
- SpreadsheetBench V2 chart-package scoring now has a narrow static lane:
  candidate and golden `.xlsx` packages can be compared across normalized
  `xl/charts/*.xml` and `xl/drawings/*.xml` parts, reporting matched, missing,
  extra, and mismatched chart parts. The workbook scorer and staged runner can
  carry that evidence in their score reports. Refreshed V2 score/run smokes now
  show missing chart/drawing package parts as `chartPackage: 0` and lower
  best-overall scores, with V2 staged/run contamination smokes still at 0 leaks.
  This proves chart-package diff plumbing; it is not rendered screenshot
  grading, layout-quality judging, or Gemini/VLM visual grading.
  `npm run benchmark:spreadsheetbench:chart-visual:probe` now records that
  stronger visual gate separately in
  `docs/eval/spreadsheetbench-chart-visual-probe.json`; the current artifact is
  `renderer_unavailable` and also records the missing screenshot pair and
  accepted VLM report. When screenshot paths are present, the same probe records
  candidate/gold image SHA-256, byte count, and PNG dimensions before accepting
  any VLM report.
- Official benchmark contamination checking now exists: `npm run
  benchmark:contamination` scans agent-facing task manifests, candidate
  manifests, and generated edit plans for evaluator-only gold/rubric/canary
  metadata. Checked-in smokes record 0 leaks for the staged SpreadsheetBench V1
  root, the N=5 V1 candidate output, the retry V1 candidate output, the V2
  staged/run outputs, and the staged BankerToolBench fixture. The Node
  permission sandbox smoke adds local subprocess fs-denial proof, and the Docker
  sandbox probe now records whether the local daemon can execute a no-network,
  read-only, agent-workspace-only container. The current probe artifact records
  `daemon_unavailable`, so it proves the blocker is real and repeatable, not
  that Docker/Harbor isolation is complete.
- BankerToolBench local proof now has both directions: the copy-input baseline
  records 0/6 weighted points with missing/extra package accounting, while the
  checked-in `apply-agent-output` smoke records 6/6 weighted points, 1/1 pass,
  candidate-before-evaluator trajectory, and 0 leaks across 4 checked files.
  `npm run benchmark:bankertoolbench:proof` enforces both artifacts. This is
  still a local exact/workbook-semantic smoke, not Harbor/Gandalf official
  verifier execution.

Gaps:

- center-stage split mode has deterministic memory-mode browser proof, but live/Convex and Gemini UI judge proof are still missing;
- the full trace still lives inside the artifact panel for detailed audit, while the bottom strip is the short shell-level summary;
- the binder hierarchy is present at MVP depth, but artifact-specific proof/policy click-throughs are not complete;
- responsive collapse has deterministic memory-mode proof, but live/Convex and Gemini UI judge proof are still missing;
- Signal Tape is implemented as a privacy-filtered, bounded aggregate strip with deterministic tests, but user filters, pause controls, and click-through drilldowns are not complete;
- PlanPreview/intake scheduling is typed, tested, and surfaced in the composer before send, but it is not yet wired into live provider spend gates;
- workbook-runtime adapter remains a POC target.
- BankerToolBench and SpreadsheetBench official adapters are not complete:
  BankerToolBench ingest/staging/gold-redaction and weighted rubric parsing are
  implemented and locally proof-gated by `npm run
  benchmark:bankertoolbench:proof`, but Harbor/Docker runner adapters,
  workbook/presentation/document deliverable packaging, Gandalf verifier score
  import, SEC/data-room/logo MCP adapters, and production weighted-rubric scoring
  remain red. SpreadsheetBench
  ingest/staging/gold-redaction, copy-input runner plumbing, deterministic
  edit-plan workbook output, a one-task passing fixture model-edit smoke, and
  one-task official V1 repeated-run and retry-policy failed/partial accounting
  smokes, local formula-result caching, static V2 chart-package comparison
  wired into score/run reports, and staged V1/V2 route-selection reports are
  implemented; the V1 route report classifies 400 staged tasks into 41
  deterministic table transforms, 218 formula edits, 33 format edits, 106
  general edits, and 2 chart-visual blockers. Full official-bundle model runs,
  full formula/format policy, rendered/VLM chart grading, Docker/Harbor sandbox
  proof, and production route execution/scoring are still missing. The Docker
  probe must report `container_isolation_proven` before this line can move.

## Implementation Sequence

P0 for June target tracking:

- keep this document canonical;
- keep production matrix rows honest;
- avoid README claims beyond verified shell/preflight scope;
- keep visible navigation aligned with Room Binder, Work Surface, Copilot, Signal Tape, and Status Strip.

P1 shell migration:

- make the artifact/workbook the primary center work surface;
- move public/private chat, job queue, agent streams, and steering into Copilot;
- add a thin shell-level status strip;
- graduate center-stage proof/source split mode from memory-mode proof to live/Convex and Gemini UI judge proof;
- implement binder sections and click semantics;
- add browser specs for `>=1440px`, `1200-1439px`, `900-1199px`, and mobile collapse.

P1 scheduler/intake:

- add typed `IntakeDecision` and `PlanPreview`;
- test affected-set expansion before model calls;
- keep lock resolution deterministic and managed by the harness;
- record cost/runtime estimates and authorization.

P2 eval packaging:

- publish a NodeRoomBench package outline with cases, artifacts, gold, runners, validators, and trust policy;
- keep benchmark-faithful adapter mode separate from product-harness eval mode.
- keep `npm run benchmark:official:readiness` green as a reporting command and
  keep `npm run benchmark:official:readiness -- --strict` red until at least one
  official adapter has produced recorded runs without hidden-gold access,
  answer lookup, benchmark hardcoding, or evaluator mutation.
- implement official adapters in this order: SpreadsheetBench V1/Verified
  full-bundle model-solving run inside the staged agent directory and score its output,
  BankerToolBench Harbor/Docker task runner around the staged manifests,
  SpreadsheetBench V2 chart/visual lane, then BankerToolBench SEC/data-room/logo
  MCP adapter import plus Gandalf verifier score ingestion.
