# Production report — stack-before-after-v1

**Rendered:** `renders/short.mp4` — 50.2s vertical, h264 + ElevenLabs narration.
**Judge: 15/16, publish** ([judge.md](./judge.md)) — one P2: the archival Streamlit frames are
dense desktop captures, small in vertical format; improvable only by re-capturing ParselyFi live
at higher zoom (queued, low priority).

First episode using the **different-stack baseline** mode of the before-scene doctrine: the
"before" is the REAL ParselyFi app (Streamlit) — excellent single-player, framed respectfully as
"where typical stacks stop by construction" (no presence, no shared versions, no review lane) —
followed by the same workflow in the live room. Baseline footage: actual frames from
HomenShum/feature-walkthrough-gif assembled into motion (the pipeline-run burst frames, not a
hero shot).

Regenerate: ffmpeg slideshow from the public repo's `public/wt/ListIntel` frames →
`voiceover.ts` → `episode.ts` → `remotion render episode-short`.
